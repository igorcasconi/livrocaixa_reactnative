import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
  cardConfig: {
    borderRadius: 20
  },
  imageCard: {
    width: 120,
    height: 120,
    minHeight: 120
  }
})

export default styles
