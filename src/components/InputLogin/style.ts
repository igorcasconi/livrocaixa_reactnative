import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    container: {
        backgroundColor: "white", paddingLeft: 10, borderRadius: 10, marginBottom: -10
    },

    label: {
        color: "white", marginBottom: 10
    }
});

export default styles;